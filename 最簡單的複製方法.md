# 最簡單的複製方法：使用 Supabase Table Editor

## 🎯 最簡單的方法（推薦）

### 步驟 1：打開 Supabase Dashboard

1. 登入 Supabase Dashboard
2. 選擇你的專案
3. 點擊左側選單的 **Table Editor**

### 步驟 2：查看 PersonnelData 的資料

1. 在 Table Editor 中選擇 **PersonnelData** 表
2. 記下或截圖每一筆記錄的：
   - `id`
   - `name`
   - `JobTitle`
   - `Mail`
   - `drive_link`

### 步驟 3：在 users 表中更新或新增

#### 情況 A：如果 users 表中已經有該 id

1. 在 Table Editor 中選擇 **users** 表
2. 找到對應的 `id` 記錄
3. 點擊該筆記錄（會進入編輯模式）
4. 修改以下欄位：
   - `name` = PersonnelData 的 `name`
   - `role` = PersonnelData 的 `JobTitle`
   - `mail` = PersonnelData 的 `Mail`
   - `headshot` = PersonnelData 的 `drive_link`
5. 點擊 **Save** 或按 **Enter**

#### 情況 B：如果 users 表中沒有該 id

1. 在 Table Editor 中選擇 **users** 表
2. 點擊右上角的 **Insert row** 或 **+** 按鈕
3. 填入以下欄位：
   - `id` = PersonnelData 的 `id`
   - `name` = PersonnelData 的 `name`
   - `role` = PersonnelData 的 `JobTitle`
   - `mail` = PersonnelData 的 `Mail`
   - `headshot` = PersonnelData 的 `drive_link`
   - `timestamp` = 可以留空或點擊自動填入
4. 點擊 **Save** 或按 **Enter**

---

## 📝 快速操作技巧

### 技巧 1：使用兩個瀏覽器視窗

1. 打開兩個瀏覽器視窗（或使用分頁）
2. 一個顯示 PersonnelData，另一個顯示 users
3. 對照著複製，避免切換視窗

### 技巧 2：使用瀏覽器的開發者工具

1. 在 PersonnelData 表中，按 **F12** 打開開發者工具
2. 在 Console 中輸入以下 JavaScript 來快速查看資料：
   ```javascript
   // 這只是範例，實際操作請直接在 Table Editor 中查看
   ```

### 技巧 3：先確認 users 表需要哪些欄位

在開始複製前，先確認 users 表有 `mail` 和 `headshot` 欄位：

1. 在 users 表中，點擊右上角的 **Edit Table** 或 **Settings**
2. 確認有 `mail` 和 `headshot` 欄位
3. 如果沒有，點擊 **Add Column** 添加：
   - `mail` (類型：TEXT)
   - `headshot` (類型：TEXT)

---

## ⚠️ 注意事項

1. **欄位對應關係**：
   - PersonnelData.`JobTitle` → users.`role`
   - PersonnelData.`Mail` → users.`mail`
   - PersonnelData.`drive_link` → users.`headshot`

2. **空值處理**：
   - 如果 PersonnelData 的某個欄位是空的，users 對應欄位也可以留空

3. **ID 衝突**：
   - 如果 users 表中已有相同的 id，直接編輯該筆記錄即可（會覆蓋原有資料）

4. **大量資料**：
   - 如果資料很多（超過 10 筆），建議使用 SQL 方法（見 `手動複製_單筆SQL範例.sql`）

---

## ✅ 完成後驗證

複製完成後，執行以下 SQL 確認：

```sql
SELECT 
  p.id,
  p.name as personnel_name,
  p."JobTitle" as personnel_role,
  u.name as user_name,
  u.role as user_role,
  u.mail,
  u.headshot
FROM "PersonnelData" p
LEFT JOIN users u ON p.id = u.id
ORDER BY p.id;
```

如果 `user_name`、`user_role`、`mail`、`headshot` 都有值，表示複製成功！
