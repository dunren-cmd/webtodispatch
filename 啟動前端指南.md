# 啟動前端應用程式指南

## 🚀 快速啟動步驟

### 步驟 1：確認 Node.js 已安裝

在 PowerShell 中執行：

```powershell
node --version
npm --version
```

如果沒有安裝，請前往：https://nodejs.org 下載並安裝 Node.js（建議使用 LTS 版本）。

### 步驟 2：安裝依賴套件

在專案資料夾中執行：

```powershell
cd "c:\Users\dunre\OneDrive\文件\請購單\WebToDispatch_2"
npm install
```

這會安裝所有必要的套件（React、Vite、TypeScript、Tailwind CSS 等）。

**注意：** 如果之前已經安裝過，可以跳過這一步。

### 步驟 3：啟動開發伺服器

```powershell
npm run dev
```

啟動成功後，你會看到類似以下的輸出：

```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3050/
  ➜  Network: http://192.168.x.x:3050/
  ➜  press h + enter to show help
```

### 步驟 4：開啟瀏覽器

根據終端機顯示的 URL，在瀏覽器中開啟：

- **本地訪問**：`http://localhost:3050`
- **網路訪問**：`http://192.168.x.x:3050`（同一網路下的其他裝置也可以訪問）

**注意：** 專案已設定為使用端口 3050（在 `vite.config.ts` 中設定）

---

## 🔧 常用命令

### 開發模式（熱重載）

```powershell
npm run dev
```

- 啟動開發伺服器
- 支援熱重載（修改程式碼後自動重新整理）
- 預設端口：3050（在 `vite.config.ts` 中設定）

### 建置生產版本

```powershell
npm run build
```

- 編譯 TypeScript
- 打包並優化程式碼
- 輸出到 `dist` 資料夾

### 預覽生產版本

```powershell
npm run preview
```

- 預覽建置後的生產版本
- 用於測試生產環境的表現

---

## ⚙️ 設定 API 連接

### 檢查 API 設定

開啟 `api.ts` 檔案，確認 `API_URL` 設定：

```typescript
// 如果使用 Google Apps Script（舊設定）
const API_URL = "你的Google Apps Script Web App URL";

// 如果使用 Supabase（新設定）
const API_URL = "http://localhost:54321";  // 本地 Supabase
// 或
const API_URL = "https://你的專案ID.supabase.co";  // 雲端 Supabase
```

### 更新 API URL

根據你的後端設定：

**選項 1：使用本地 Supabase**
```typescript
const API_URL = "http://localhost:54321";
```

**選項 2：使用雲端 Supabase**
```typescript
const API_URL = "https://你的專案ID.supabase.co";
```

**選項 3：使用 Google Apps Script**
```typescript
const API_URL = "https://script.google.com/macros/s/你的腳本ID/exec";
```

---

## 🐛 常見問題

### 問題 1：`npm install` 失敗

**錯誤訊息：** `npm ERR!` 或 `ENOENT`

**解決方案：**

1. **清除 npm 快取**
   ```powershell
   npm cache clean --force
   ```

2. **刪除 node_modules 和 package-lock.json**
   ```powershell
   Remove-Item -Recurse -Force node_modules
   Remove-Item package-lock.json
   ```

3. **重新安裝**
   ```powershell
   npm install
   ```

### 問題 2：端口 3050 已被佔用

**錯誤訊息：** `Port 3050 is in use`

**解決方案：**

1. **使用其他端口**
   ```powershell
   npm run dev -- --port 3000
   ```
   或修改 `vite.config.ts` 中的 `port: 3050` 為其他端口

2. **或停止佔用端口的程式**
   ```powershell
   netstat -ano | findstr :3050
   taskkill /PID <PID> /F
   ```

### 問題 3：無法連接到後端 API

**錯誤訊息：** `Failed to fetch` 或 `CORS error`

**檢查清單：**

1. ✅ 確認後端服務正在運行
   - Supabase：執行 `supabase status` 確認運行中
   - Google Apps Script：確認已部署為 Web App

2. ✅ 確認 API URL 正確
   - 檢查 `api.ts` 中的 `API_URL`
   - 確認 URL 格式正確

3. ✅ 檢查瀏覽器 Console
   - 按 `F12` 開啟開發者工具
   - 查看 Console 標籤的錯誤訊息
   - 查看 Network 標籤的請求狀態

4. ✅ 確認 CORS 設定
   - Google Apps Script：確認 Web App 設定為「任何人」可以訪問
   - Supabase：確認 RLS 政策允許訪問

### 問題 4：樣式沒有顯示

**解決方案：**

1. **確認 Tailwind CSS 已安裝**
   ```powershell
   npm list tailwindcss
   ```

2. **重新啟動開發伺服器**
   ```powershell
   # 按 Ctrl+C 停止
   npm run dev
   ```

3. **檢查 `src/index.css`**
   - 確認包含 Tailwind 指令：
     ```css
     @tailwind base;
     @tailwind components;
     @tailwind utilities;
     ```

### 問題 5：TypeScript 編譯錯誤

**解決方案：**

1. **檢查 TypeScript 版本**
   ```powershell
   npm list typescript
   ```

2. **重新安裝 TypeScript**
   ```powershell
   npm install --save-dev typescript@latest
   ```

3. **檢查 `tsconfig.json` 設定**

---

## 📝 開發提示

### 熱重載

- 修改 `src/` 資料夾中的檔案會自動重新載入
- 不需要手動重新整理瀏覽器
- 如果修改沒有生效，嘗試手動重新整理（`Ctrl+R` 或 `F5`）

### 瀏覽器開發者工具

- **Console**：查看 JavaScript 錯誤和日誌
- **Network**：查看 API 請求和回應
- **Elements**：檢查 HTML 結構和 CSS 樣式

### 程式碼結構

```
src/
├── App.tsx          # 主要應用程式元件
├── main.tsx          # 應用程式入口點
└── index.css         # 全域樣式（包含 Tailwind）
```

---

## 🎯 完整啟動流程

### 第一次啟動

1. **安裝 Node.js**（如果還沒有）
   - 下載：https://nodejs.org
   - 安裝 LTS 版本

2. **安裝專案依賴**
   ```powershell
   cd "c:\Users\dunre\OneDrive\文件\請購單\WebToDispatch_2"
   npm install
   ```

3. **確認後端運行**
   - Supabase：執行 `supabase status` 確認運行中
   - 或確認 Google Apps Script 已部署

4. **設定 API URL**
   - 編輯 `api.ts`，設定正確的 `API_URL`

5. **啟動前端**
   ```powershell
   npm run dev
   ```

6. **開啟瀏覽器**
   - 訪問 `http://localhost:3050`

### 之後的啟動

只需要執行：

```powershell
cd "c:\Users\dunre\OneDrive\文件\請購單\WebToDispatch_2"
npm run dev
```

---

## ✅ 快速檢查清單

- [ ] Node.js 已安裝（`node --version`）
- [ ] npm 已安裝（`npm --version`）
- [ ] 依賴套件已安裝（`npm install`）
- [ ] 後端服務正在運行（Supabase 或 Google Apps Script）
- [ ] API URL 已正確設定（`api.ts`）
- [ ] 開發伺服器已啟動（`npm run dev`）
- [ ] 瀏覽器可以訪問（`http://localhost:3050`）

---

## 📚 相關資源

- Vite 文件：https://vitejs.dev/
- React 文件：https://react.dev/
- TypeScript 文件：https://www.typescriptlang.org/
- Tailwind CSS 文件：https://tailwindcss.com/
