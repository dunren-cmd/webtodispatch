# CSV 檔案複製說明

## 📋 情況說明

CSV 檔案在 Windows 系統上（`c:\Users\dunre\Desktop\`），需要複製到 Linux 系統才能匯入。

## 🔧 複製方法

### 方法 1：使用 SCP（推薦，如果兩台電腦在同一網路）

在 Windows PowerShell 中執行：

```powershell
# 連接到 Linux 系統並複製檔案
scp "c:\Users\dunre\Desktop\roles_rows.csv" user@linux-ip:/home/dunren/cursor/webtodispatch/WebToDispatch_2/
scp "c:\Users\dunre\Desktop\users_rows (2).csv" user@linux-ip:/home/dunren/cursor/webtodispatch/WebToDispatch_2/users_rows.csv
scp "c:\Users\dunre\Desktop\tasks_rows.csv" user@linux-ip:/home/dunren/cursor/webtodispatch/WebToDispatch_2/
```

### 方法 2：使用共享資料夾

如果 Linux 可以存取 Windows 共享資料夾：

```bash
# 在 Linux 上
cp /path/to/shared/roles_rows.csv /home/dunren/cursor/webtodispatch/WebToDispatch_2/
cp "/path/to/shared/users_rows (2).csv" /home/dunren/cursor/webtodispatch/WebToDispatch_2/users_rows.csv
cp /path/to/shared/tasks_rows.csv /home/dunren/cursor/webtodispatch/WebToDispatch_2/
```

### 方法 3：使用 USB 隨身碟

1. 將 CSV 檔案複製到 USB 隨身碟
2. 在 Linux 上掛載 USB 隨身碟
3. 複製檔案：

```bash
# 掛載 USB（通常會在 /media/ 或 /mnt/）
sudo mount /dev/sdb1 /mnt/usb

# 複製檔案
cp /mnt/usb/roles_rows.csv /home/dunren/cursor/webtodispatch/WebToDispatch_2/
cp "/mnt/usb/users_rows (2).csv" /home/dunren/cursor/webtodispatch/WebToDispatch_2/users_rows.csv
cp /mnt/usb/tasks_rows.csv /home/dunren/cursor/webtodispatch/WebToDispatch_2/
```

### 方法 4：使用 Python 腳本直接讀取（如果 Python 可以存取 Windows 路徑）

如果是在 WSL 或可以存取 Windows 路徑的環境：

```bash
cd /home/dunren/cursor/webtodispatch/WebToDispatch_2

python3 import_all_csv_to_supabase.py \
  --roles "/mnt/c/Users/dunre/Desktop/roles_rows.csv" \
  --users "/mnt/c/Users/dunre/Desktop/users_rows (2).csv" \
  --tasks "/mnt/c/Users/dunre/Desktop/tasks_rows.csv"
```

### 方法 5：手動複製（最簡單）

1. 在 Windows 上開啟檔案總管
2. 找到 CSV 檔案：`c:\Users\dunre\Desktop\`
3. 複製檔案
4. 在 Linux 系統上，使用檔案管理器或終端機貼上到目標目錄

## 📁 目標目錄

檔案應該複製到：

```
/home/dunren/cursor/webtodispatch/WebToDispatch_2/
```

## ✅ 複製完成後

複製完成後，執行匯入：

```bash
cd /home/dunren/cursor/webtodispatch/WebToDispatch_2

python3 import_all_csv_to_supabase.py \
  --roles roles_rows.csv \
  --users users_rows.csv \
  --tasks tasks_rows.csv
```

## 🔍 檢查檔案是否已複製

```bash
cd /home/dunren/cursor/webtodispatch/WebToDispatch_2
ls -la *.csv
```

應該看到：
- `roles_rows.csv`
- `users_rows.csv`
- `tasks_rows.csv`

