# 應用程式啟動指南

## ✅ 當前狀態

- ✓ Supabase 本地服務運行中
- ✓ 環境變數已配置 (.env)
- ✓ Vite 開發服務器已啟動

## 🚀 訪問應用程式

應用程式正在啟動中，請稍等片刻後訪問：

**預設地址**: http://localhost:5173

如果端口被占用，Vite 會自動選擇下一個可用端口（5174, 5175 等）。

## 📋 檢查應用程式狀態

### 方法 1: 查看終端輸出
Vite 啟動後會顯示類似以下的輸出：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

### 方法 2: 檢查進程
```bash
ps aux | grep vite
```

### 方法 3: 檢查端口
```bash
# 查看 Vite 監聽的端口
lsof -i -P -n | grep vite
# 或
netstat -tlnp | grep 5173
```

## 🔧 如果應用程式無法啟動

### 檢查 Supabase 是否運行
```bash
cd /home/dunren/cursor/webtodispatch/WebToDispatch_2
npx supabase status
```

如果 Supabase 未運行，啟動它：
```bash
npx supabase start
```

### 檢查環境變數
```bash
cat .env
```

應該包含：
```
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
```

### 重新啟動應用程式
```bash
# 停止當前進程（如果在運行）
pkill -f "vite --host"

# 重新啟動
npm run dev
```

## 📝 常用命令

### 開發模式
```bash
npm run dev
```

### 構建生產版本
```bash
npm run build
```

### 預覽生產版本
```bash
npm run preview
```

## 🌐 服務地址總結

- **前端應用**: http://localhost:5173 (或 Vite 顯示的端口)
- **Supabase Studio**: http://127.0.0.1:54323
- **Supabase API**: http://127.0.0.1:54321
- **Mailpit**: http://127.0.0.1:54324

## ⚠️ 注意事項

1. **首次啟動**: 應用程式首次啟動可能需要一些時間來編譯
2. **端口衝突**: 如果 5173 端口被占用，Vite 會自動選擇其他端口
3. **網絡訪問**: 使用 `--host` 參數，可以通過網絡 IP 訪問（如 http://192.168.x.x:5173）
4. **熱重載**: Vite 支持熱模塊替換（HMR），修改代碼後會自動刷新

---

**項目位置**: `/home/dunren/cursor/webtodispatch/WebToDispatch_2`



