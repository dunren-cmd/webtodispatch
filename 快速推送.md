# 快速推送到 Git

## 方法 1：使用批次檔（推薦）

直接雙擊執行 `git_push.bat` 檔案即可。

## 方法 2：手動執行命令

開啟命令提示字元（CMD）或 PowerShell，執行：

```cmd
cd /d "c:\Users\dunre\OneDrive\文件\請購單\WebToDispatch_2"
git add .
git commit -m "更新：新增 Supabase migration - 將 level 5 改為 level 4"
git push origin main
```

## 本次更新內容

- ✅ 新增 migration：`20251213120000_update_level5_to_level4.sql`
- ✅ 更新說明文件

## 遠端倉庫資訊

- 倉庫：`git@github.com:Dunren-Hospital/WebToDispatch_2.git`
- 分支：`main`
